general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore

      
## Deployment options

deployment:
  default:
    branch: [master]
    commands:
      - bower install
      - grunt publish
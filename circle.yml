dependencies:
  pre:
    - git config user.email "a.herz@freegroup.de"
    - git config user.name "FreeGroup"


## Deployment options

deployment:
  default:
    branch: [master]
    commands:
      - grunt publish